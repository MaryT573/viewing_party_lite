<div>
  <table>
    <thead>
      <tr>
        <th>Users</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.username, user_path(user) %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= link_to "Create New User", new_user_path %>

